package ru.tusur.presentation.about

import androidx.lifecycle.ViewModel

// ---------------------------------------------------------
// ViewModel для экрана "О приложении"
// ---------------------------------------------------------
// Содержит статическую информацию о версии приложения
// Не требует состояния или бизнес-логики, так как данные не изменяются
class AboutViewModel : ViewModel() {
    // ---------------------------------------------------------
    // Строка с версией приложения
    // ---------------------------------------------------------
    // Формат: v{version} (Build {date})
    // Отображается на экране AboutScreen
    val version: String = "v1.0.0 (Build 20251219)"
}