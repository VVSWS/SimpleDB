package ru.tusur.core.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

// ---------------------------------------------------------
// Конфигурация форм скругления для Material 3
// ---------------------------------------------------------
// Определяет стандартные радиусы скругления для компонентов UI
// Используется в теме приложения для единообразия дизайна
// Размеры соответствуют спецификации Material Design 3
val AppShapes = Shapes(
    // ---------------------------------------------------------
    // Очень маленькие элементы
    // ---------------------------------------------------------
    // Применяется: чипы, маленькие кнопки, индикаторы, тултипы
    extraSmall = RoundedCornerShape(4.dp),

    // ---------------------------------------------------------
    // Маленькие элементы
    // ---------------------------------------------------------
    // Применяется: карточки с одним действием, кнопки, поля ввода
    small = RoundedCornerShape(8.dp),

    // ---------------------------------------------------------
    // Средние элементы
    // ---------------------------------------------------------
    // Применяется: стандартные карточки, диалоги, модальные окна
    medium = RoundedCornerShape(12.dp),

    // ---------------------------------------------------------
    // Крупные элементы
    // ---------------------------------------------------------
    // Применяется: крупные карточки, панели, нижние листы (BottomSheet)
    large = RoundedCornerShape(16.dp),

    // ---------------------------------------------------------
    // Очень крупные элементы
    // ---------------------------------------------------------
    // Применяется: полноэкранные диалоги, боковые панели, крупные модалы
    extraLarge = RoundedCornerShape(28.dp)
)